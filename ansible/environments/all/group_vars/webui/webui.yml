ansible_ssh_private_key_file: "{{ 'files/keys/' + remote_user + '@' + remote_servername if ( lookup('ansible.builtin.fileglob', 'files/keys/' + remote_user + '@' + remote_servername) != []) else user_ssh_priv_key }}"